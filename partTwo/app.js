const cat = "Shadow";
const age = 4;
let isBlack = true;

console.log (cat, age, isBlack);
/**
 * type of: allows you to see what value that variable holds [is a string, number boolean etc]
 * 
 */
console.log (typeof cat, typeof age, typeof isBlack);
